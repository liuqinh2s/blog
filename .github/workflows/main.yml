name: deploying Hexo project to GitHub pages
on:
  push:
    branches:
      - master # master 分支有 push 行为时就触发这个 action

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@master
        
      - name: Hexo Action
        # You may pin to the exact commit or the version.
        # uses: sma11black/hexo-action@08f5143fa402cdaccf9fc016d6bc91329f082eb0
        uses: sma11black/hexo-action@v1.0.4
        with:
          # Github user name
          user_name: liuqinh2s  # default is github-actions[bot]
          # Github email
          user_email: liuqinh2s@gmail.com # default is 41898282+github-actions[bot]@users.noreply.github.com
          # The deploy key for Github Pages repo
          deploy_key: ${{ secrets.HEXO_DEPLOY }} # default is 
          # Git commit messages to your GitHub Pages repository.
#           commit_msg: update # optional, default is 

#       - name: Build and Deploy
#         uses: theme-keep/hexo-deploy-github-pages-action@master # 使用专门部署 Hexo 到 GitHub pages 的 action
#         env:
#           PERSONAL_TOKEN: ${{ secrets.HEXO_DEPLOY }} # secret 名
#           PUBLISH_REPOSITORY: liuqinh2s/blog # 公共仓库，格式：GitHub 用户名/仓库名
#           BRANCH: gh-pages # 分支，填 gh-pages 就行
#           PUBLISH_DIR: ./docs # 部署 public 目录下的文件
